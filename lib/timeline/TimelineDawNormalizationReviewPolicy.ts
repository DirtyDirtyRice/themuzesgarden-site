import { createHash } from"node:crypto";
export type TimelineDawNormalizationReview={songId:string;decision:"pending"|"confirmed"|"corrected";key:string;mode:"major"|"minor";bpm:number;note:string};
export function parseTimelineDawNormalizationReviews(value:unknown,knownIds:ReadonlySet<string>):TimelineDawNormalizationReview[]{if(!Array.isArray(value))throw Error("Normalization reviews are required.");const seen=new Set<string>();return value.map((raw,index)=>{if(!raw||typeof raw!=="object")throw Error(`Review ${index+1} is invalid.`);const x=raw as Record<string,unknown>,songId=String(x.songId??"").trim(),decision=String(x.decision??"pending") as TimelineDawNormalizationReview["decision"],key=String(x.key??"").trim().toUpperCase(),mode=x.mode,bpm=Number(x.bpm),note=String(x.note??"").trim();if(!knownIds.has(songId)||seen.has(songId))throw Error(`Review ${index+1} source is invalid.`);seen.add(songId);if(!["pending","confirmed","corrected"].includes(decision))throw Error(`Review ${index+1} decision is invalid.`);if(!/^[A-G](#)?$/.test(key)||(mode!=="major"&&mode!=="minor"))throw Error(`Review ${index+1} key is invalid.`);if(!Number.isFinite(bpm)||bpm<40||bpm>240)throw Error(`Review ${index+1} tempo is invalid.`);if(note.length>500)throw Error(`Review ${index+1} note is too long.`);return{songId,decision,key,mode,bpm:Math.round(bpm*10)/10,note}})}
export function timelineDawNormalizationRevisionChecksum(input:{targetBpm:number;targetKey:string;targetMode:string;reviews:TimelineDawNormalizationReview[]}){return`sha256:${createHash("sha256").update(JSON.stringify(input)).digest("hex")}`}
export function assertTimelineDawNormalizationPromotion(reviews:TimelineDawNormalizationReview[]){const pending=reviews.filter(x=>x.decision==="pending");if(pending.length)throw Error(`Confirm or correct ${pending.length} normalization review${pending.length===1?"":"s"} before promotion.`)}