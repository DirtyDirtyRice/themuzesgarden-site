export function timelineDawNormalizationEscalation(input:{dueAt:string;lastNotifiedAt?:string|null;level?:number;now:Date}){const overdueHours=Math.max(0,(input.now.getTime()-Date.parse(input.dueAt))/3600000),level=overdueHours>=72?3:overdueHours>=24?2:1,cooldownHours={1:24,2:12,3:6}[level]??24,last=input.lastNotifiedAt?Date.parse(input.lastNotifiedAt):0;return{level,cooldownHours,notify:!last||input.now.getTime()-last>=cooldownHours*3600000,idempotencyKey:`${input.dueAt}:L${level}:${Math.floor(input.now.getTime()/(cooldownHours*3600000))}`}}export function timelineDawNormalizationWorkerReadiness(input:{cronSecret:boolean;serviceRole:boolean;scheduleConfigured:boolean}){const missing=[!input.cronSecret&&"CRON_SECRET",!input.serviceRole&&"SUPABASE_SERVICE_ROLE_KEY",!input.scheduleConfigured&&"deployment schedule"].filter(Boolean);return{ready:missing.length===0,missing}}
