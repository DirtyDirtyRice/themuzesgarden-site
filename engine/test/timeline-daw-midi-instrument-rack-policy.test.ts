import{describe,expect,it}from"vitest";import{parseTimelineDawMidiAutomation,parseTimelineDawMidiRack,renderTimelineDawMidiRack,timelineDawMidiAutomationValue,timelineDawMidiRackRecipeChecksum}from"../../lib/timeline/TimelineDawMidiInstrumentRackPolicy";import{parseTimelineDawPrivateMidiClip}from"../../lib/timeline/TimelineDawPrivateMidiPolicy";
const rack=parseTimelineDawMidiRack({name:"Wide Keys",version:2,layers:[{id:"body",waveform:"triangle",gain:.3,pan:-.2,filterHz:8000},{id:"air",waveform:"sine",octave:1,gain:.1,pan:.2,filterHz:12000}]},"rack");const automation=parseTimelineDawMidiAutomation([{id:"expression",parameter:"expression",channel:1,points:[{id:"a",tick:0,value:.5},{id:"b",tick:480,value:1}]}],960);const clip=parseTimelineDawPrivateMidiClip({lengthTicks:960,notes:[{id:"n",pitch:60,velocity:100,startTick:0,durationTicks:480,channel:1}]},"clip");
describe("MIDI instrument rack",()=>{it("validates layered presets and versions",()=>{expect(rack.layers).toHaveLength(2);expect(rack.version).toBe(2);expect(()=>parseTimelineDawMidiRack({layers:[]})).toThrow(/1 to 8/)});it("interpolates editable automation curves",()=>expect(timelineDawMidiAutomationValue(automation[0],240,0)).toBeCloseTo(.75));it("renders deterministic bounded stereo layers",()=>{const a=renderTimelineDawMidiRack(clip,rack,automation,8000,[{tick:0,bpm:120}]),b=renderTimelineDawMidiRack(clip,rack,automation,8000,[{tick:0,bpm:120}]);expect(a.channels).toHaveLength(2);expect(Array.from(a.channels[0])).toEqual(Array.from(b.channels[0]));expect(Math.max(...a.channels[0])).toBeLessThanOrEqual(1)});it("tracks rack and automation freshness",()=>expect(timelineDawMidiRackRecipeChecksum({rack,automation})).not.toBe(timelineDawMidiRackRecipeChecksum({rack:{...rack,version:3},automation})))});