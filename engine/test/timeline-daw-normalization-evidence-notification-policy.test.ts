import{describe,expect,it}from"vitest";import{createTimelineDawNormalizationEvidenceDeliveryReceipt,createTimelineDawNormalizationEvidenceNotification,timelineDawNormalizationEvidenceDeliveryPlan,timelineDawNormalizationEvidenceEscalation}from"../../lib/timeline/TimelineDawNormalizationEvidenceNotificationPolicy";
describe("evidence incident notifications",()=>{it("escalates integrity failures faster",()=>{const now=new Date("2026-08-14T12:00:00Z"),createdAt="2026-08-14T06:00:00Z";expect(timelineDawNormalizationEvidenceEscalation({issue:"chain-hash",createdAt,now}).level).toBe(2);expect(timelineDawNormalizationEvidenceEscalation({issue:"coverage-gap",createdAt,now}).level).toBe(1)});it("backs off and dead-letters the fifth failure",()=>{expect(timelineDawNormalizationEvidenceDeliveryPlan({attemptCount:0,now:new Date(0)})).toMatchObject({attempt:1,delayMinutes:5,deadLetter:false});expect(timelineDawNormalizationEvidenceDeliveryPlan({attemptCount:4,now:new Date(0)})).toMatchObject({attempt:5,deadLetter:true,nextAttemptAt:null})});it("protects links and receipts with checksums",()=>{const n=createTimelineDawNormalizationEvidenceNotification({incidentId:"i",checkpointId:"c",sessionId:"s",projectId:"p",issue:"chronology",level:2,severity:"critical"});expect(n.href).toBe("/workspace/projects/p/studio/s");expect(n.checksum).toMatch(/^sha256:/);expect(createTimelineDawNormalizationEvidenceDeliveryReceipt({notificationId:"n"}).checksum).toMatch(/^sha256:/)})});
