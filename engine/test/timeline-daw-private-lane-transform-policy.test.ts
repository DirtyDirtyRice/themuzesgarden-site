import{describe,expect,it}from"vitest";import{parseTimelineDawPrivateLaneTransform,timelineDawPrivateLanePlaybackRate,transformTimelineDawPrivateLanePcm}from"../../lib/timeline/TimelineDawPrivateLaneTransformPolicy";describe("lane transforms",()=>{it("validates bounded reversible parameters",()=>{expect(parseTimelineDawPrivateLaneTransform({stretchRatio:2,pitchSemitones:-12,algorithm:"preserve-pitch",bypassed:false})).toMatchObject({stretchRatio:2});expect(()=>parseTimelineDawPrivateLaneTransform({stretchRatio:9,pitchSemitones:0,algorithm:"resample"})).toThrow(/0.25/)});it("derives synchronized preview rates",()=>{expect(timelineDawPrivateLanePlaybackRate({stretchRatio:2,pitchSemitones:12,algorithm:"resample",bypassed:false})).toBeCloseTo(1);expect(timelineDawPrivateLanePlaybackRate({stretchRatio:2,pitchSemitones:12,algorithm:"preserve-pitch",bypassed:false})).toBe(.5)});it("renders stretched PCM without changing its source",()=>{const source=new Float32Array([0,1,0,-1]),out=transformTimelineDawPrivateLanePcm([source],{stretchRatio:2,pitchSemitones:0,algorithm:"preserve-pitch",bypassed:false});expect(out[0]).toHaveLength(8);expect([...source]).toEqual([0,1,0,-1])})});
