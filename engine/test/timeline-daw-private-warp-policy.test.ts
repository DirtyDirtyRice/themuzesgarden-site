import{describe,expect,it}from"vitest";import{applyTimelineDawPrivateGroove,extractTimelineDawPrivateGroove,moveTimelineDawPrivateWarpMarker,parseTimelineDawPrivateWarpMarkers,quantizeTimelineDawPrivateWarpMarkers}from"../../lib/timeline/TimelineDawPrivateWarpPolicy";import{createTimelineDawElasticPlan}from"../../lib/timeline/TimelineDawPrivateLaneTransformPolicy";const markers=[{sourceFrame:100,destinationFrame:110,protected:false},{sourceFrame:200,destinationFrame:230,protected:true}];describe("private warp maps",()=>{it("rejects collisions and non-monotonic edits",()=>{expect(()=>parseTimelineDawPrivateWarpMarkers([{sourceFrame:100,destinationFrame:200},{sourceFrame:200,destinationFrame:100}],1000)).toThrow(/order/);expect(()=>moveTimelineDawPrivateWarpMarker(markers,100,240)).toThrow(/order/)});it("quantizes selected markers without moving protected attacks",()=>{expect(quantizeTimelineDawPrivateWarpMarkers(markers,100,.5,[100,200])).toEqual([{...markers[0],destinationFrame:105},markers[1]])});it("extracts and applies reusable groove offsets",()=>{const groove={id:"g",...extractTimelineDawPrivateGroove("Push",markers,100)};expect(applyTimelineDawPrivateGroove([{sourceFrame:100,destinationFrame:100,protected:false}],groove,1)[0].destinationFrame).toBe(110)});it("uses explicit warp destinations in the shared elastic plan",()=>{const plan=createTimelineDawElasticPlan(1000,48000,{stretchRatio:1,pitchSemitones:0,algorithm:"preserve-pitch",quality:"balanced",bypassed:false},[],markers);expect(plan.anchors.slice(1,3)).toEqual([{sourceFrame:100,outputFrame:110},{sourceFrame:200,outputFrame:230}])})});
