import{describe,expect,it}from"vitest";import{assertTimelineDawNormalizationSupportConsent,assertTimelineDawNormalizationSupportCaseTransition,BUNDLE_SCHEMA,createTimelineDawNormalizationDiagnosticBundle}from"../../lib/timeline/TimelineDawNormalizationSupportCasePolicy";describe("normalization support case policy",()=>{it("creates a privacy-reviewed bundle without audio or paths",()=>{const bundle=createTimelineDawNormalizationDiagnosticBundle({report:{schema:"report",checksum:"sha256:x",sessionId:"s",revisions:[]},recoveryReceipts:[{id:"r",retention_receipt_id:"p",after_failures:[],recovered_paths:["private/path"],created_at:"2026-08-13T00:00:00Z"}]});expect(bundle.manifest.schema).toBe(BUNDLE_SCHEMA);expect(bundle.manifest.containsAudio).toBe(false);expect(JSON.stringify(bundle)).not.toContain("private/path");expect(bundle.checksum).toMatch(/^sha256:[a-f0-9]{64}$/)});it("requires consent and enforces lifecycle transitions",()=>{expect(()=>assertTimelineDawNormalizationSupportConsent(false)).toThrow(/consent/);expect(()=>assertTimelineDawNormalizationSupportCaseTransition("open","resolved")).not.toThrow();expect(()=>assertTimelineDawNormalizationSupportCaseTransition("revoked","open")).toThrow(/cannot move/)})});
