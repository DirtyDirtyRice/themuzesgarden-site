import{describe,expect,it}from"vitest";import{timelineDawNormalizationRetentionEligibility,timelineDawNormalizationSupportSla,timelineDawNormalizationSupportTriage}from"../../lib/timeline/TimelineDawNormalizationSupportTriagePolicy";describe("normalization support triage",()=>{it("assigns deterministic priority and SLA",()=>{const now=new Date("2026-08-13T00:00:00Z"),triage=timelineDawNormalizationSupportTriage({alerts:[{severity:"critical"}],remainingRecoveryFailures:0,now});expect(triage).toEqual({priority:"critical",dueAt:"2026-08-13T04:00:00.000Z",slaHours:4});expect(timelineDawNormalizationSupportSla(triage.dueAt,"open",new Date("2026-08-13T05:00:00Z"))).toBe("overdue")});it("only cleans expired or revoked payloads",()=>{expect(timelineDawNormalizationRetentionEligibility({state:"open",retainUntil:"2026-08-14T00:00:00Z",now:new Date("2026-08-13T00:00:00Z")})).toBe(false);expect(timelineDawNormalizationRetentionEligibility({state:"revoked",retainUntil:"2026-09-13T00:00:00Z"})).toBe(true)})});