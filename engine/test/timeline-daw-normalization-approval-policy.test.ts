import{describe,expect,it}from"vitest";import{assertTimelineDawNormalizationPrunable,isTimelineDawNormalizationApprovalCurrent,timelineDawNormalizationApprovalChecksum}from"../../lib/timeline/TimelineDawNormalizationApprovalPolicy";const d={revision:2,proofChecksum:"p",recipeChecksums:["r2","r1"],artifactChecksums:["a2","a1"],masterChecksum:"m"};describe("normalization approval ledger",()=>{it("binds approval to sorted audible dependencies",()=>{const checksum=timelineDawNormalizationApprovalChecksum(d);expect(checksum).toMatch(/^sha256:/);expect(isTimelineDawNormalizationApprovalCurrent(checksum,{...d,recipeChecksums:["r1","r2"],artifactChecksums:["a1","a2"]})).toBe(true);expect(isTimelineDawNormalizationApprovalCurrent(checksum,{...d,masterChecksum:"changed"})).toBe(false)});it("protects current and approved artifacts from pruning",()=>{expect(()=>assertTimelineDawNormalizationPrunable({revision:1,currentRevision:2,approved:false,retentionDecision:"prune"})).not.toThrow();expect(()=>assertTimelineDawNormalizationPrunable({revision:2,currentRevision:2,approved:false,retentionDecision:"prune"})).toThrow(/Current/);expect(()=>assertTimelineDawNormalizationPrunable({revision:1,currentRevision:2,approved:true,retentionDecision:"prune"})).toThrow(/Approved/)})});