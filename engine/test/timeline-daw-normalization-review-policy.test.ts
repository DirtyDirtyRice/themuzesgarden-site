import{describe,expect,it}from"vitest";import{assertTimelineDawNormalizationPromotion,parseTimelineDawNormalizationReviews,timelineDawNormalizationRevisionChecksum}from"../../lib/timeline/TimelineDawNormalizationReviewPolicy";const ids=new Set(["a","b"]);describe("normalization review decisions",()=>{it("persists confirmed and corrected evidence",()=>{const reviews=parseTimelineDawNormalizationReviews([{songId:"a",decision:"confirmed",key:"C#",mode:"minor",bpm:88,note:"heard"},{songId:"b",decision:"corrected",key:"D",mode:"minor",bpm:90,note:"bass resolves to D"}],ids);expect(()=>assertTimelineDawNormalizationPromotion(reviews)).not.toThrow();expect(timelineDawNormalizationRevisionChecksum({targetBpm:88,targetKey:"C#",targetMode:"minor",reviews})).toMatch(/^sha256:/)});it("blocks promotion while a listening review is pending",()=>{const reviews=parseTimelineDawNormalizationReviews([{songId:"a",decision:"pending",key:"C",mode:"major",bpm:80,note:""}],ids);expect(()=>assertTimelineDawNormalizationPromotion(reviews)).toThrow(/Confirm or correct/)})});