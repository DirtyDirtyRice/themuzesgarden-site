import{describe,expect,it}from"vitest";import{applyTimelineDawVersionOffset,isTimelineDawVersionAlignmentStale,parseTimelineDawVersionAlignment,suggestTimelineDawVersionAlignment,timelineDawAlignmentReceiptChecksum}from"../../lib/timeline/TimelineDawVersionAlignmentPolicy";const checksum="sha256:"+"a".repeat(64);
describe("version alignment",()=>{it("suggests onset-correlated offsets with confidence",()=>{const result=suggestTimelineDawVersionAlignment([0,0,1,.5,0,0],[0,1,.5,0,0,0],.1);expect(result.offsetSeconds).toBeCloseTo(.1);expect(result.confidence).toBeCloseTo(1)});it("validates manual confirmation and non-destructive placement",()=>{const alignment=parseTimelineDawVersionAlignment({versionId:"v",sourceChecksum:checksum,offsetSeconds:-2,confidence:.8,method:"manual",confirmed:true,revision:1});expect(applyTimelineDawVersionOffset(5,alignment)).toBe(3);expect(applyTimelineDawVersionOffset(1,alignment)).toBe(0)});it("detects source staleness and stable receipts",()=>{const alignment=parseTimelineDawVersionAlignment({versionId:"v",sourceChecksum:checksum,offsetSeconds:0,confidence:1,method:"manual"});expect(isTimelineDawVersionAlignmentStale(alignment,"sha256:"+"b".repeat(64))).toBe(true);expect(timelineDawAlignmentReceiptChecksum(alignment)).toMatch(/^sha256:[a-f0-9]{64}$/)})});