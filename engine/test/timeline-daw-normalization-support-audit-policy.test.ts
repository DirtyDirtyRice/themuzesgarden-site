import{describe,expect,it}from"vitest";import{createTimelineDawNormalizationSupportAudit,parseTimelineDawNormalizationSupportAudit,timelineDawNormalizationSupportAuditCompleteness,timelineDawNormalizationSupportCasesCsv}from"../../lib/timeline/TimelineDawNormalizationSupportAuditPolicy";describe("normalization support audit",()=>{it("validates checksum and rejects tampering",()=>{const audit=createTimelineDawNormalizationSupportAudit({cases:[],summary:{total:0}});expect(parseTimelineDawNormalizationSupportAudit(audit).checksum).toBe(audit.checksum);expect(()=>parseTimelineDawNormalizationSupportAudit({...audit,summary:{total:1}})).toThrow(/checksum/)});it("finds missing and broken lifecycle references",()=>expect(timelineDawNormalizationSupportAuditCompleteness({cases:[{id:"a"}],history:[],notifications:[{case_id:"missing"}],escalations:[]})).toEqual({complete:false,findings:["a: missing creation receipt","notification: broken case reference missing"]}));it("exports metadata CSV with escaped summaries",()=>expect(timelineDawNormalizationSupportCasesCsv([{id:"a",summary:'one, "two"'}])).toContain('"one, ""two"""'))});
