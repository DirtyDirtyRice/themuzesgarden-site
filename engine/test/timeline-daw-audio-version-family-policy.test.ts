import{describe,expect,it}from"vitest";import{normalizedTimelineDawFamilyLaneStart,parseTimelineDawIntakeCandidate,planTimelineDawBatchIntake,summarizeTimelineDawIntake}from"../../lib/timeline/TimelineDawAudioVersionFamilyPolicy";const checksum=(char:string)=>"sha256:"+char.repeat(64),candidate=(id:string,role="demo")=>({id,name:id+".mp3",byteLength:100,checksum:checksum(id),role,version:"v1",relationship:"alternate"});
describe("audio version family intake",()=>{it("validates explicit metadata without inferring ownership",()=>expect(parseTimelineDawIntakeCandidate(candidate("a"))).toMatchObject({performer:null,origin:null,familyId:null}));it("deduplicates by canonical checksum and bounds concurrency",()=>{const plan=planTimelineDawBatchIntake([candidate("a"),candidate("b"),candidate("a")],[],2);expect(plan.accepted).toHaveLength(2);expect(plan.duplicates).toHaveLength(1);expect(plan.batches.map(x=>x.length)).toEqual([2])});it("summarizes recoverable outcomes by role",()=>{const plan=planTimelineDawBatchIntake([candidate("a","acapella"),candidate("b","stem")]);expect(summarizeTimelineDawIntake(plan.accepted,[],1)).toMatchObject({total:3,accepted:2,failed:1,byRole:{acapella:1,stem:1}})});it("places family versions aligned or sequentially",()=>{expect(normalizedTimelineDawFamilyLaneStart(2,"aligned",[2,3,4])).toBe(0);expect(normalizedTimelineDawFamilyLaneStart(2,"sequential",[2,3,4])).toBe(5)})});