import{describe,expect,it}from"vitest";import{reconcileTimelineDawNormalizationConditions,timelineDawNormalizationStorageTotals}from"../../lib/timeline/TimelineDawNormalizationReconciliationPolicy";describe("normalization reconciliation",()=>{it("creates stable condition keys and clears absent conditions",()=>{const conditions=reconcileTimelineDawNormalizationConditions({now:new Date("2026-08-14T01:00:00Z"),cronLastRun:"2026-08-14T00:00:00Z",cronConfigured:true,jobs:[{id:"j",state:"rendering",leaseExpiresAt:"2026-08-14T00:59:00Z"}],approvalCurrent:false,pruneFailures:[]});expect(conditions.map(x=>x.key)).toEqual(["cron","lease:j","approval"]);expect(reconcileTimelineDawNormalizationConditions({now:new Date(),cronLastRun:new Date().toISOString(),cronConfigured:true,jobs:[],approvalCurrent:true,pruneFailures:[]})).toEqual([])});it("accounts artifact classes separately",()=>{expect(timelineDawNormalizationStorageTotals({jobs:[{revision:1,sourceByteLength:10,artifactByteLength:20,retained:true},{revision:1,sourceByteLength:10,artifactByteLength:30,retained:false}],masters:[{byteLength:40}]})).toEqual({sourceBytes:10,laneRenderBytes:50,masterBytes:40,retainedBytes:20})})});