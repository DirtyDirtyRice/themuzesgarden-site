import{describe,expect,it}from"vitest";import{applyTimelineDawNormalizationAuditRepairFindings,assertTimelineDawNormalizationAuditRepairPlan,createTimelineDawNormalizationAuditRepairPlan,timelineDawNormalizationAuditChain}from"../../lib/timeline/TimelineDawNormalizationSupportAuditRepairPolicy";describe("support audit repair",()=>{it("creates conflict-checked repair plans",()=>{const plan=createTimelineDawNormalizationAuditRepairPlan("sha256:base",["case-1: missing creation receipt"]);expect(plan.actions[0]).toMatchObject({kind:"append-creation-provenance",caseId:"case-1"});expect(()=>assertTimelineDawNormalizationAuditRepairPlan(plan,"sha256:base")).not.toThrow();expect(()=>assertTimelineDawNormalizationAuditRepairPlan(plan,"sha256:new")).toThrow(/stale/)});it("chains evidence and computes after findings",()=>{const a=timelineDawNormalizationAuditChain(null,{type:"export",checksum:"x"}),b=timelineDawNormalizationAuditChain(a,{type:"repair",checksum:"y"});expect(a).not.toBe(b);expect(applyTimelineDawNormalizationAuditRepairFindings(["a","b"],[{kind:"document-broken-reference",finding:"a"}])).toEqual(["b"])})});
