import{describe,expect,it}from"vitest";import{timelineDawNormalizationSupportAutomationPlan,timelineDawNormalizationSupportWorkerHealth}from"../../lib/timeline/TimelineDawNormalizationSupportAutomationPolicy";describe("normalization support automation",()=>{it("plans idempotent overdue notification and cleanup",()=>{const p=timelineDawNormalizationSupportAutomationPlan({id:"c",state:"open",dueAt:"2026-08-12T00:00:00Z",retainUntil:"2026-08-12T00:00:00Z",now:new Date("2026-08-13T00:00:00Z")});expect(p).toMatchObject({overdue:true,cleanup:true,notify:true,escalationKey:"c:2026-08-12T00:00:00Z"})});it("does not repeat cleaned payloads or pending notifications",()=>{const p=timelineDawNormalizationSupportAutomationPlan({id:"c",state:"open",dueAt:"2026-08-12T00:00:00Z",retainUntil:"2026-08-12T00:00:00Z",bundleCleanedAt:"2026-08-12T01:00:00Z",notificationState:"pending",now:new Date("2026-08-13T00:00:00Z")});expect(p.cleanup).toBe(false);expect(p.notify).toBe(false)});it("reports stale worker health",()=>expect(timelineDawNormalizationSupportWorkerHealth("2026-08-13T00:00:00Z",new Date("2026-08-13T01:00:00Z"),true).healthy).toBe(false))});
