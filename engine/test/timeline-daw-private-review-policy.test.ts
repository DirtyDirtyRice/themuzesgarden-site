import{describe,expect,it}from"vitest";import{canEditTimelineDawPrivateReview,extractTimelineDawPrivateMentions,locateTimelineDawPrivateReviewAnchor,parseTimelineDawPrivateReviewAnchor,splitTimelineDawPrivateReviewAnchor}from"../../lib/timeline/TimelineDawPrivateReviewPolicy";const anchor=parseTimelineDawPrivateReviewAnchor({targetKind:"lane",targetId:"lane",startSample:1000,endSample:1200,sourceFrame:500,tempoTick:960});describe("private review anchors",()=>{it("validates exact point and range targets",()=>{expect(anchor).toMatchObject({sourceFrame:500,tempoTick:960});expect(()=>parseTimelineDawPrivateReviewAnchor({targetKind:"bus",targetId:"",startSample:10,endSample:5})).toThrow(/invalid/)});it("keeps source-relative comments aligned through lane moves",()=>{expect(locateTimelineDawPrivateReviewAnchor(anchor,{timelineStartSample:10000,sourceInFrame:400,sourceOutFrame:1000})).toEqual({startSample:10100,endSample:10300})});it("moves anchors to the correct child after a split",()=>{expect(splitTimelineDawPrivateReviewAnchor(anchor,"left","right",600).targetId).toBe("left");expect(splitTimelineDawPrivateReviewAnchor({...anchor,sourceFrame:700},"left","right",600).targetId).toBe("right")});it("deduplicates mentions and enforces role-aware edits",()=>{expect(extractTimelineDawPrivateMentions("@Alex check with @alex and @Blair")).toEqual(["alex","blair"]);expect(canEditTimelineDawPrivateReview("editor","a","a")).toBe(true);expect(canEditTimelineDawPrivateReview("editor","a","b")).toBe(false);expect(canEditTimelineDawPrivateReview("owner","a","b")).toBe(true)})});
