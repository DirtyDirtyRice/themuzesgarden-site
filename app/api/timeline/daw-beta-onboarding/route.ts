import crypto from"node:crypto";import{createClient}from"@supabase/supabase-js";import{NextRequest,NextResponse}from"next/server";import{createTimelineDawBetaInviteCode,createTimelineDawBetaOnboardingReceipt,evaluateTimelineDawBetaRelease,hashTimelineDawBetaInviteCode,parseTimelineDawBetaEnvironment,TIMELINE_DAW_BETA_ACKNOWLEDGEMENT_VERSION}from"@/lib/timeline/TimelineDawBetaOnboardingPolicy";import{createTimelineDawWorkspaceServer}from"@/lib/timeline/TimelineDawWorkspaceServer";export const runtime="nodejs",dynamic="force-dynamic";
const env=(n:string)=>{const v=process.env[n]?.trim();if(!v)throw Error(`${n} is not configured.`);return v};async function auth(r:NextRequest){const h=r.headers.get("authorization")??"";if(!h.toLowerCase().startsWith("bearer "))throw Error("Authentication is required.");const token=h.slice(7).trim(),client=createClient(env("NEXT_PUBLIC_SUPABASE_URL"),env("NEXT_PUBLIC_SUPABASE_ANON_KEY"),{auth:{persistSession:false,autoRefreshToken:false},global:{headers:{Authorization:`Bearer ${token}`}}}),{data,error}=await client.auth.getUser(token);if(error||!data.user)throw Error("Supabase session is invalid or expired.");return{id:data.user.id,token,client}}
async function owner(r:NextRequest,sessionId:string){const u=await auth(r),session=await createTimelineDawWorkspaceServer(u.id,u.token).get(u.id,sessionId);if(!session)throw Error("Only the verified session owner can manage this beta.");return{...u,session}}
async function ownerData(u:Awaited<ReturnType<typeof owner>>,sessionId:string){const[{data:invitations,error:iError},{data:enrollments,error:eError},{data:workflow,error:wError},{data:feedback,error:fError},{data:incidents,error:xError},{data:receipts,error:rError}]=await Promise.all([u.client.from("timeline_daw_beta_invitations").select("id,label,state,expires_at,created_at").eq("owner_id",u.id).eq("session_id",sessionId).order("created_at",{ascending:false}),u.client.from("timeline_daw_beta_enrollments").select("*").eq("owner_id",u.id).eq("session_id",sessionId).order("created_at",{ascending:false}),u.client.from("timeline_daw_beta_workflow_receipts").select("evaluation").eq("owner_id",u.id).eq("session_id",sessionId).order("observed_at",{ascending:false}).limit(1).maybeSingle(),u.client.from("timeline_daw_beta_feedback").select("severity,state").eq("owner_id",u.id).eq("session_id",sessionId),u.client.from("timeline_daw_normalization_evidence_incidents").select("state").eq("owner_id",u.id).eq("session_id",sessionId).eq("state","manual-review"),u.client.from("timeline_daw_beta_release_receipts").select("*").eq("owner_id",u.id).eq("session_id",sessionId).order("observed_at",{ascending:false}).limit(12)]);const error=iError||eError||wError||fError||xError||rError;if(error)throw Error(error.message);const evaluation=(workflow?.evaluation??{percent:0,complete:false,exportReady:false})as{percent:number;complete:boolean;exportReady:boolean};return{invitations:invitations??[],enrollments:enrollments??[],workflow:evaluation,feedback:feedback??[],integrityBlockers:(incidents??[]).length,releaseReceipts:receipts??[]}}
const fail=(e:unknown)=>NextResponse.json({error:e instanceof Error?e.message:"Beta onboarding request failed."},{status:400});export async function GET(r:NextRequest){try{const sessionId=r.nextUrl.searchParams.get("sessionId")?.trim()??"",u=await owner(r,sessionId);return NextResponse.json(await ownerData(u,sessionId),{headers:{"Cache-Control":"no-store"}})}catch(e){return fail(e)}}
export async function POST(r:NextRequest){try{const b=await r.json(),action=String(b.action??"");if(action==="redeem"){const u=await auth(r),code=String(b.code??"").trim();if(code.length<20)throw Error("Invitation code is invalid.");const{data,error}=await u.client.rpc("redeem_timeline_daw_beta_invitation",{p_code_hash:hashTimelineDawBetaInviteCode(code)});if(error)throw Error(error.message);const{data:enrollment}=await u.client.from("timeline_daw_beta_enrollments").select("id,session_id,project_id,state").eq("id",String(data)).eq("tester_id",u.id).single();if(!enrollment)throw Error("Enrollment could not be loaded.");return NextResponse.json({enrollmentId:enrollment.id,sessionId:enrollment.session_id,projectId:enrollment.project_id,state:enrollment.state})}if(action==="acknowledge"||action==="environment"){const u=await auth(r),environment=action==="environment"?parseTimelineDawBetaEnvironment(b.environment):null,{data,error}=await u.client.rpc("save_timeline_daw_beta_tester_onboarding",{p_enrollment_id:String(b.enrollmentId??""),p_action:action,p_acknowledgement_version:TIMELINE_DAW_BETA_ACKNOWLEDGEMENT_VERSION,p_environment:environment});if(error)throw Error(error.message);if(!data)throw Error("Active tester enrollment was not found.");return NextResponse.json({updated:true})}const sessionId=String(b.sessionId??"").trim(),u=await owner(r,sessionId);if(action==="invite"){const label=String(b.label??"").trim();if(label.length<2||label.length>100)throw Error("Tester label must contain 2-100 characters.");const code=createTimelineDawBetaInviteCode(),expiresAt=new Date(Date.now()+7*86400000).toISOString(),{data,error}=await u.client.from("timeline_daw_beta_invitations").insert({id:`timeline-daw-beta-invite-${crypto.randomUUID()}`,owner_id:u.id,project_id:u.session.projectId,session_id:sessionId,label,invite_code_hash:hashTimelineDawBetaInviteCode(code),expires_at:expiresAt}).select("id,label,state,expires_at,created_at").single();if(error)throw Error(error.message);return NextResponse.json({invitation:data,code})}if(action==="revoke"){const id=String(b.invitationId??""),{error}=await u.client.from("timeline_daw_beta_invitations").update({state:"revoked",updated_at:new Date().toISOString()}).eq("id",id).eq("owner_id",u.id).eq("session_id",sessionId);if(error)throw Error(error.message);await u.client.from("timeline_daw_beta_enrollments").update({state:"revoked",updated_at:new Date().toISOString()}).eq("invitation_id",id).eq("owner_id",u.id);return NextResponse.json(await ownerData(u,sessionId))}if(action==="release"){const snapshot=await ownerData(u,sessionId),enrollment=snapshot.enrollments.find(x=>x.id===String(b.enrollmentId??""));if(!enrollment)throw Error("Beta enrollment was not found.");const open=snapshot.feedback.filter(x=>x.state!=="resolved"),evaluation=evaluateTimelineDawBetaRelease({enrolled:enrollment.state==="active",acknowledged:enrollment.acknowledgement_version===TIMELINE_DAW_BETA_ACKNOWLEDGEMENT_VERSION,environment:parseTimelineDawBetaEnvironment(enrollment.environment),workflowPercent:snapshot.workflow.percent,workflowComplete:snapshot.workflow.complete,exportReady:snapshot.workflow.exportReady,blockingFeedback:open.filter(x=>x.severity==="blocking").length,unresolvedFeedback:open.length,integrityBlockers:snapshot.integrityBlockers}),observedAt=new Date().toISOString(),receipt=createTimelineDawBetaOnboardingReceipt({sessionId,enrollmentId:enrollment.id,evaluation,observedAt}),{error}=await u.client.from("timeline_daw_beta_release_receipts").insert({id:`timeline-daw-beta-release-${crypto.randomUUID()}`,owner_id:u.id,session_id:sessionId,enrollment_id:enrollment.id,ready:evaluation.ready,evaluation,receipt_checksum:receipt.checksum,observed_at:observedAt});if(error)throw Error(error.message);return NextResponse.json({...await ownerData(u,sessionId),release:evaluation})}throw Error("Beta onboarding action is invalid.")}catch(e){return fail(e)}}
