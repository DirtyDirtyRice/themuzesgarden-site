"use client";import{useState}from"react";import Link from"next/link";import{useAuth}from"@/app/components/AuthProvider";import{requireProjectSupabase}from"@/app/workspace/projects/[id]/projectSupabase";type Enrollment={enrollmentId:string;sessionId:string;projectId:string;state:string};const field="rounded-lg border border-white/15 bg-black px-3 py-2",button="rounded-lg border border-white/20 bg-white px-3 py-2 font-black text-black disabled:opacity-40";
export default function DawBetaEnrollmentPage(){const{user,loading}=useAuth(),[code,setCode]=useState(""),[enrollment,setEnrollment]=useState<Enrollment|null>(null),[acknowledged,setAcknowledged]=useState(false),[environment,setEnvironment]=useState<Record<string,boolean>|null>(null),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function post(body:Record<string,unknown>){setBusy(true);setError("");try{const token=(await requireProjectSupabase().auth.getSession()).data.session?.access_token??"",r=await fetch("/api/timeline/daw-beta-onboarding",{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify(body)}),j=await r.json();if(!r.ok)throw Error(j.error);return j}catch(e){setError(e instanceof Error?e.message:"Onboarding request failed.");throw e}finally{setBusy(false)}}async function redeem(){const result=await post({action:"redeem",code});setEnrollment(result);setCode("")}async function acknowledge(){if(!enrollment)return;await post({action:"acknowledge",enrollmentId:enrollment.enrollmentId,sessionId:enrollment.sessionId});setAcknowledged(true)}async function check(){if(!enrollment)return;let audioInput=false;try{const stream=await navigator.mediaDevices.getUserMedia({audio:true});audioInput=stream.getAudioTracks().length>0;stream.getTracks().forEach(track=>track.stop())}catch{audioInput=false}let localStorageReady=false;try{localStorage.setItem("muzes-beta-check","1");localStorage.removeItem("muzes-beta-check");localStorageReady=true}catch{localStorageReady=false}const result={secureContext:window.isSecureContext,supportedBrowser:Boolean(window.AudioContext&&window.MediaRecorder),audioInput,audioOutput:Boolean(window.HTMLAudioElement),localStorage:localStorageReady,fileApi:Boolean(window.File&&window.FileReader),supportedAudioTypes:Boolean(document.createElement("audio").canPlayType("audio/wav")||document.createElement("audio").canPlayType("audio/mpeg"))};await post({action:"environment",enrollmentId:enrollment.enrollmentId,sessionId:enrollment.sessionId,environment:result});setEnvironment(result)}if(loading)return <main className="p-8 text-white">Checking membership…</main>;if(!user)return <main className="p-8 text-white"><h1 className="text-3xl font-black">Musician Beta Enrollment</h1><p className="my-4">Sign in before redeeming an invitation.</p><Link className={button} href="/members">Members Sign In</Link></main>;return <main className="mx-auto max-w-3xl space-y-5 p-6 text-white"><h1 className="text-4xl font-black">Musician Beta Enrollment</h1><p className="text-white/60">This is experimental software. Keep your original audio backups. Testing may expose unfinished behavior. Private audio is not attached to feedback reports.</p>{!enrollment?<div className="flex gap-2"><input className={field} value={code} onChange={e=>setCode(e.target.value)} placeholder="Invitation code"/><button className={button} disabled={busy||code.length<20} onClick={()=>void redeem()}>Redeem invitation</button></div>:<><section className="rounded-2xl border border-white/15 p-4"><h2 className="font-black">1. Privacy and beta-risk acknowledgement</h2><label className="mt-2 block"><input type="checkbox" checked={acknowledged} onChange={e=>setAcknowledged(e.target.checked)}/> I understand this is beta software, I will keep backups, and feedback excludes my audio and protected diagnostics.</label><button className={`${button} mt-2`} disabled={busy||!acknowledged} onClick={()=>void acknowledge()}>Save acknowledgement</button></section><section className="rounded-2xl border border-white/15 p-4"><h2 className="font-black">2. Browser and audio setup check</h2><p className="text-white/55">Your browser may ask for microphone permission. The test stops the microphone immediately after confirming an input exists.</p><button className={`${button} mt-2`} disabled={busy} onClick={()=>void check()}>Run environment check</button>{environment?Object.entries(environment).map(([name,ready])=><p className={ready?"text-emerald-200":"text-red-200"} key={name}>{ready?"Pass":"Needs attention"} · {name}</p>):null}</section><section className="rounded-2xl border border-amber-300/30 p-4"><b>Enrollment active</b><p>The owner must run the release gate before this session opens. The link below rechecks every requirement and revocation immediately.</p><Link className={`${button} mt-3 inline-block`} href={`/workspace/daw/beta/session/${encodeURIComponent(enrollment.sessionId)}`}>Check access and open session</Link></section></>}{error?<p role="alert" className="text-red-200">{error}</p>:null}</main>}
